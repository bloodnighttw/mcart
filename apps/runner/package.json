{
    "name": "@mcart/runner",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "cargo build --release",
        "dev": "bacon run",
        "lint": "cargo clippy -- -D warnings",
        "check-types": "cargo check --all-targets",
        "install": "cargo install --locked bacon && cargo install --path ."
    }
}